<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:productsViews="clr-namespace:MealRandomizer.Views.ProductsViews"
             x:Class="MealRandomizer.Views.ProductsViews.AllProductsPage"
             Title="{Binding Title}"
             BackgroundColor="{StaticResource Primary}">
    <ContentPage.ToolbarItems>
        <ToolbarItem IconImageSource="round_add_24.xml"/>
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <Grid RowSpacing="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="25"/>
            </Grid.RowDefinitions>
            <productsViews:ProductsHeaderView Grid.Row="0"/>
            <productsViews:ProductsView Grid.Row="1"/>
        </Grid>
    </ContentPage.Content>
</ContentPage>